[core/cli]
task_name = core/task/supervised
depends_libraries = ['unitorch_microsoft.adsplus.image.bletchley_v1']
from_ckpt_dir = ./cache
cache_dir = ./cache
train_file = ./train.tsv
dev_file = ./dev.tsv
test_file = ./test.tsv

# model
[microsoft/adsplus/image/bletchley/v1]
config_type = 0.3B
projection_dim = 64
hidden_size = 512
num_text_layers = 4
freeze_base_model = True
freeze_image_model = True
gradient_checkpointing = True
pretrained_weight_path = https://unitorchblobfuse.blob.core.windows.net/shares/models/bletchley/v1/pytorch_model.0.3B.bin

# dataset
[core/dataset/ast]
# id, image, text
names = ['query', 'image', 'label']

[core/dataset/ast/train]
data_files = ${core/cli:train_file}
names = ['query', 'image', 'score']
preprocess_functions = [
    'microsoft/process/bletchley/v1/classification(query, core/process/image/read(image))',
    'core/process/label(score, dtype="float")',
  ]

[core/dataset/ast/dev]
data_files = ${core/cli:dev_file}
preprocess_functions = [
    'microsoft/process/bletchley/v1/classification(query, core/process/image/read(image))',
    'core/process/label(label)',
  ]

[core/dataset/ast/test]
data_files = ${core/cli:test_file}
preprocess_functions = [
    'microsoft/process/bletchley/v1/classification(query, core/process/image/read(image))',
  ]

[core/process/image]
http_url = http://0.0.0.0:11230/?image={0}

# process
[microsoft/process/bletchley/v1]
max_seq_length = 36

# optim
[core/optim/adamw]
learning_rate = 0.00001

# scheduler
[core/scheduler/linear_warmup]
num_warmup_rate = 0.001

# task
[core/task/supervised]
model = microsoft/adsplus/image/bletchley/v1
dataset = core/dataset/ast
optim = core/optim/adamw
scheduler = core/scheduler/linear_warmup
loss_fn = core/loss/mse
score_fn = core/score/auc
monitor_fns = ['core/score/auc']
output_header = ['query', 'image', 'label']
postprocess_fn = core/postprocess/classification/binary_score
writer = core/writer/csv

from_ckpt_dir = ${core/cli:from_ckpt_dir}
to_ckpt_dir = ${core/cli:cache_dir}
output_path = ${core/cli:cache_dir}/output.txt

num_workers = 16
log_freq = 10
ckpt_freq = 15

train_batch_size = 128
dev_batch_size = 128
test_batch_size = 128