[core/cli]
task_name = core/task/supervised
depends_libraries = ['unitorch_microsoft.models.tulr', 'unitorch_microsoft.msan.l1']
from_ckpt_dir = ./cache
cache_dir = ./cache
train_file = ./train.tsv
dev_file = ./dev.tsv
test_file = ./test.tsv

# model
[microsoft/msan/l1/classification/bletchley/v1]
config_type = 0.15B
projection_dim = 32
enable_tgs_features = True
enable_demands_features = True
enable_markets_features = False
enable_positions_features = False
output_user_embeds = True
pretrained_weight_path = https://unitorchblobfuse.blob.core.windows.net/shares/models/msan/pytorch_model.msan.l1.bletchley.v1.0.796.bin

# dataset
[core/dataset/ast]
names = ['rowid', 'user_id', 'tg_id', 'demand_type_id', 'market_id', 'ad_position_id', 'uet_events', 'search_events', 'ad_keyword', 'ad_title', 'ad_url', 'ad_desc', 'ad_sn', 'label']

[core/dataset/ast/test]
data_files = ${core/cli:test_file}
preprocess_functions = [
  '''
    microsoft/msan/l1/process/bletchley/v1/text_classification/v2(
      (search_events.split(" #$$# ") if search_events else []) + (uet_events.split(" #$$# ") if uet_events else []),
      prefix="user_",
    )
  ''',
  'core/process/number(tg_id[1:], dtype="int", key="tg_ids")',
  'core/process/number(demand_type_id[1:], dtype="int", key="demand_ids")',
  'core/process/number(market_id[1:], dtype="int", key="market_ids")',
  'core/process/number(ad_position_id[1:], dtype="int", key="pos_ids")',
  ]

# process
[microsoft/msan/l1/process/bletchley/v1]
max_num_text = 10
max_seq_length = 64

[microsoft/process/bletchley/v1]
max_seq_length = 64

# task
[core/task/supervised]
model = microsoft/msan/l1/classification/bletchley/v1
dataset = core/dataset/ast
output_header = ['rowid']
postprocess_fn = core/postprocess/classification/embedding
writer = core/writer/parquet

num_workers = 16
postprocess_workers = 4

from_ckpt_dir = ${core/cli:from_ckpt_dir}
to_ckpt_dir = ${core/cli:cache_dir}
output_path = ${core/cli:cache_dir}/output.parquet

test_batch_size = 256
