[core/cli]
depends_libraries = [
    "unitorch_microsoft.models.diffusers",
    "unitorch_microsoft.omnipixel.webuis.outpainting",
  ]
enabled_webuis = [
    "microsoft/omnipixel/webui/outpainting",
  ]
device = cpu

[core/pipeline/stable_flux/inpainting]
enable_cpu_offload = True

device = ${core/cli:device}
