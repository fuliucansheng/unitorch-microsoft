[core/cli]
task_name = core/task/supervised
depends_libraries = ['unitorch_microsoft.vpr']
from_ckpt_dir = ./cache
cache_dir = ./cache
train_file = ./train.tsv
dev_file = ./dev.tsv

# model
[microsoft/vpr/classification/bert/v2]
pretrained_name = bert-base-uncased
config_path = vpr/configs/bert.argus.json
num_hidden_layers = 3
image_embed_dim = 100

# dataset
[core/dataset/ast]
names = ['id', 'image', 'text', 'label']

[core/dataset/ast/test]
data_files = ${core/cli:test_file}
preprocess_functions = [
    'core/process/bert/classification(text)',
    'core/process/features(image, pad=0, num=100, dtype="float", key="image_embeds")'
  ]

# process
[core/process/bert]
pretrained_name = bert-base-uncased
max_seq_length = 48

# task
[core/task/supervised]
model = microsoft/vpr/classification/bert/v2
dataset = core/dataset/ast

output_header = ['id', 'image', 'text', 'label']
postprocess_fn = core/postprocess/classification/binary_score
writer = core/writer/csv

from_ckpt_dir = ${core/cli:from_ckpt_dir}
to_ckpt_dir = ${core/cli:cache_dir}

num_workers = 16

train_batch_size = 128
dev_batch_size = 128
